/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'jsdelivr.net', 'cdn.jsdelivr.net', 'ceecee.ir']
    }
};

export default nextConfig;
